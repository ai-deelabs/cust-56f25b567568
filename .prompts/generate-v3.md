Build a complete landing page as the single file `v3.html`.

Brand:
เฟอนิเซิร์ฟ is a brand of cleaning products specializing in upholstery cleaners. They want an organic and herbal look, with a green color palette that conveys freshness and natural qualities.

This is variant 3 of 3. You decide the whole design —
palette, typography, layout, spacing, mood, imagery. Design it specifically for
THIS brand: let its industry, audience, energy, and personality drive every
choice. Derive the color palette from the brand's character — never fall back to
a generic safe look or a default beige/cream editorial template. Avoid cliché
color associations (gold=luxury, blue=trust, green=health).

Make each of the 3 variants a genuinely different design take:
vary the layout structure, the type pairing, and the color treatment between
variants so the customer has real choices — not the same template recolored.
If the brand text above is short, invent plausible concrete details (sections,
copy, imagery) rather than thinning the page.

REQUIRED SECTIONS (a hero alone is NOT acceptable — build the full page):
- Hero with a clear headline + primary CTA
- Services / features / products (cards or grid)
- A trust/social-proof section (testimonials, stats, logos, or gallery)
- An about / story section
- A final call-to-action band
- Footer with contact details

Set `<html lang="en">`. Add icons with `<i data-lucide="icon-name"></i>`.

Images — search with different keywords per section:
`bash /home/runner/work/web-builder-control/web-builder-control/core/tools/search-images.sh "keyword" [count]`
Use returned URLs in `<img>` with `?w=WIDTH&h=HEIGHT&fit=crop`.
Fallback: `https://picsum.photos/seed/{keyword}/{width}/{height}`.
